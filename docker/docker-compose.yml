services:
  openclaw:
    image: ghcr.io/openclaw/openclaw:latest
    container_name: clawguard-openclaw
    volumes:
      - ./openclaw-defaults/openclaw.json:/home/node/.openclaw/openclaw.json
      - openclaw-data:/home/node/.openclaw/skills
    networks:
      - clawguard-net
    read_only: true
    tmpfs:
      - /tmp
      - /home/node/.openclaw/tmp:uid=1000,gid=1000
      - /home/node/.openclaw/canvas:uid=1000,gid=1000
      - /home/node/.openclaw/cron:uid=1000,gid=1000
      - /home/node/.openclaw/workspace:uid=1000,gid=1000
      - /home/node/.openclaw/agents:uid=1000,gid=1000
    depends_on:
      - ollama
    environment:
      - OLLAMA_HOST=http://ollama:11434

  ollama:
    image: ollama/ollama:latest
    container_name: clawguard-ollama
    volumes:
      - ollama-models:/root/.ollama
    networks:
      - clawguard-net
    ports:
      - "11434:11434"

  dashboard:
    build:
      context: ..
      dockerfile: docker/Dockerfile.dashboard
    container_name: clawguard-dashboard
    ports:
      - "3000:3000"
    volumes:
      - ./openclaw-defaults:/config
      - ../test-skills:/test-skills:ro
    networks:
      - clawguard-net
    environment:
      - NODE_ENV=production
      - OPENCLAW_CONFIG_PATH=/config/openclaw.json

networks:
  clawguard-net:
    driver: bridge

volumes:
  openclaw-data:
  ollama-models:
