services:
  openclaw:
    image: ghcr.io/openclaw/openclaw:latest  # placeholder — Phase 2 will retarget to a real agent image
    container_name: boltclaw-openclaw
    volumes:
      - ./openclaw-defaults/openclaw.json:/home/node/.openclaw/openclaw.json
      - openclaw-data:/home/node/.openclaw/skills
    networks:
      - boltclaw-net
    read_only: true
    tmpfs:
      - /tmp
      - /home/node/.openclaw/tmp:uid=1000,gid=1000
      - /home/node/.openclaw/canvas:uid=1000,gid=1000
      - /home/node/.openclaw/cron:uid=1000,gid=1000
      - /home/node/.openclaw/workspace:uid=1000,gid=1000
      - /home/node/.openclaw/agents:uid=1000,gid=1000
    depends_on:
      - ollama
    environment:
      - OLLAMA_HOST=http://ollama:11434

  ollama:
    image: ollama/ollama:latest
    container_name: boltclaw-ollama
    volumes:
      - ollama-models:/root/.ollama
    networks:
      - boltclaw-net
    ports:
      - "11434:11434"

  dashboard:
    build:
      context: ..
      dockerfile: docker/Dockerfile.dashboard
    container_name: boltclaw-dashboard
    ports:
      - "3000:3000"
    volumes:
      - ./openclaw-defaults:/config
      - ../test-skills:/test-skills:ro
    networks:
      - boltclaw-net
    environment:
      - NODE_ENV=production
      - OPENCLAW_CONFIG_PATH=/config/openclaw.json

networks:
  boltclaw-net:
    driver: bridge

volumes:
  openclaw-data:
  ollama-models:
